package dev.slimevr.localizer

import dev.slimevr.config.Settings
import dev.slimevr.skeleton.Skeleton

class LocalizerBasicBehaviour(val settings: Settings, val skeleton: Skeleton) : LocalizerBehaviour {
    override fun observe(receiver: LocalizerManager) {
    }
}
